#Convert data to a list of colours
data2colours<-function(data,data.window,thresh,rampPalette){

rampPalette(length(data[which(data.window>thresh,arr.in=T)]))[rank(data[which(data.window>thresh,arr.in=T)])]")

}
